require('dressing').setup({
    input = {
        border = vim.g.lin_constants.ui.border,
    },
    select = {
        nui = {
            border = {
                style = vim.g.lin_constants.ui.border,
            },
        },
        builtin = {
            border = vim.g.lin_constants.ui.border,
        }
    }
})
