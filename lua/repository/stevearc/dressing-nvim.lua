require('dressing').setup({
    input = {
        border = vim.g.lin_globals_ui_border,
    },
    select = {
        nui = {
            border = {
                style = vim.g.lin_globals_ui_border,
            },
        },
        builtin = {
            border = vim.g.lin_globals_ui_border,
        }
    }
})
