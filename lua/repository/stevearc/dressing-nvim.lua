require('dressing').setup({
    input = {
        border = vim.g.lin_globals_border,
    },
    select = {
        nui = {
            border = {
                style = vim.g.lin_globals_border,
            },
        },
        builtin = {
            border = vim.g.lin_globals_border,
        }
    }
})
